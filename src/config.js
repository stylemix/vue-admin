const Config = {
  /**
   * Main callbacks for auth forms: login, register, forgot, logout
   */
  authHandler: null,

  /**
   * Default (home) route
   */
  defaultRoute: null,
}

export default Config
